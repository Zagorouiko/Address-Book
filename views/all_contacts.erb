<h1>All Contacts</h1>
<ul>
  <% @contacts.each() do |contact| %>
  <li><a href="/contact/<%= contact.id() %>"><%= contact.first_name() %> <%= contact.last_name() %> <%= contact.birthday() %></a></li>
  <% end %>
</ul>
